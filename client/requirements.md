## Packages
framer-motion | Essential for smooth, premium entry animations and staggered lists
date-fns | Required for formatting article creation dates beautifully

## Notes
The application relies on Shadcn UI components which are assumed to be pre-installed in `@/components/ui/*`.
The sidebar layout follows the requested Shadcn Sidebar guidelines.
Using Lucide React for consistent agentic iconography.
